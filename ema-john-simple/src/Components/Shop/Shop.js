import React from 'react';
import fakeData from '../../fakeData/products.JSON'

const Shop = () => {
    console.log(fakeData);
    return (
        <div>
            this is shop house
        </div>
    );
};

export default Shop;